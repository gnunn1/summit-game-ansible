################################################
#  Minishift variables
################################################
master_url: https://127.0.0.1:8443
username: developer
password: developer

admin_username: admin
admin_password: admin

################################################
# ocplab.com variables
################################################

# master_url: https://ocplab.com:8443
# username: user04
# password: dummy

# admin_username: admin
# admin_password: dummy

project_name: summit-game

# Authorization token to use for admin client
auth_token: CH2UsJePthRWTmLI8EY6

# The number of replicas to use for each color. If this is set to 2
# then blue will have 2 and green will have 2 for 4 total.
gamebus_replica_count: 2

# If you are installing on something other then a single node
# cluster, set to true to deploy javascript apps in parallel
parallel_build: false

# Score server
score_user: kieserver
score_password: ki3server@

# Maven repository - set this to use a Maven Mirror for builds
# maven_mirror_url: http://nexus.nexus:8081/content/groups/public

# Repositories
game_server_repo: https://github.com/gnunn1/vertx-game-server

# Repo below instead of Burr's fixes hibernate issue when compiling
score_server_repo: https://github.com/gnunn1/score-server

# Note the achievement server was re-written from EAP to vert.x, this playbook expects the vert.x version
achievement_server_repo: https://github.com/burrsutter/vertx-achievement-service

# Repo uses a URL based on location so it doesn't have to be changed anymore
mobile_app_repo: https://github.com/gnunn1/mobile-app

# Repo uses a URL based on location so it doesn't have to be changed anymore
mobile_admin_app_repo: https://github.com/gnunn1/mobile-app-admin

# Repo uses a URL based on location so it doesn't have to be changed anymore
scoreboard_repo: https://github.com/gnunn1/scoreboard

# Repo uses a URL based on location so it doesn't have to be changed anymore
leaderboard_repo: https://github.com/gnunn1/leaderboard
